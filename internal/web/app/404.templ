package appsrc

import (
	"blog/internal/web/appcore"
	webi18n "blog/internal/web/i18n"
)

templ Page(view appcore.RootLayoutView, path string) {
	<section class="not-found-page">
		<article class="not-found-card panel">
			<p class="not-found-kicker">{ appcore.Message(view.MessagesMap(), webi18n.KeyNotfoundKicker) }</p>
			<h1 class="not-found-title">{ appcore.Message(view.MessagesMap(), webi18n.KeyNotfoundTitle) }</h1>
			<p class="not-found-summary">
				{ appcore.Message(view.MessagesMap(), webi18n.KeyNotfoundSummaryPrefix) }
				<code class="not-found-path">{ path }</code>
				{ appcore.Message(view.MessagesMap(), webi18n.KeyNotfoundSummarySuffix) }
			</p>
			<div class="not-found-actions">
				<a class="channels-back-button" href={ appcore.LocalizeAppPath(view.LocaleCode(), "/") }>{ appcore.Message(view.MessagesMap(), webi18n.KeyNotfoundBack) }</a>
				<a class="channels-back-button not-found-alt-action" href={ appcore.LocalizeAppPath(view.LocaleCode(), "/channels") }>{ appcore.Message(view.MessagesMap(), webi18n.KeyNotfoundOpenChannels) }</a>
			</div>
		</article>
	</section>
}
